node {
  stage ("Checkout") {
    echo "Checkout"
    def mvnHome = tool name: 'Default', type: 'hudson.tasks.Maven$MavenInstallation'
    echo "mvnHome = $mvnHome"
  }

  stage ("Build") {
    echo "Build"
  }
}
